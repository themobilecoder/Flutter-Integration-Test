name: flutter_integration_test
description: Sample app with integration tests

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3
  http: ^0.12.1
  cached_network_image: ^2.2.0+1


dev_dependencies:
  flutter_driver:
    sdk: flutter
  test: any
  
flutter:

  uses-material-design: true
